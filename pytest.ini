[pytest]
pythonpath = src
testpaths = tests
asyncio_mode = auto
addopts = --import-mode=importlib
asyncio_default_fixture_loop_scope = function

# Markers for test organization
markers =
    integration: marks tests as integration tests (requires USPTO_API_KEY)